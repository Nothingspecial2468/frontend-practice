# Color Palette Generator

A simple app to generate random color palettes and copy hex values.

## Features

- Generate random colors
- Allows to copy hex values to clipboard
- Responsive Layout

## Tech Used:

- HTML
- CSS(Grid, Flexbox, Transitions)
- JavaScript(DOM, Events, Clipboard API)